interface:
  display_name: "KirokuForge"
  short_description: "Initialize and resume Markdown project memory"
  default_prompt: "Use $kiroku-forge to initialize or resume this project’s durable Markdown memory."
