interface:
  display_name: "KirokuForge"
  short_description: "Build provenance-backed project memory"
  default_prompt: "Use $kiroku-forge to turn this project context into validated durable memory."
